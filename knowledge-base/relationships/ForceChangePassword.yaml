name: ForceChangePassword
description: Source can reset the target account password.
source_types: [User, Group]
target_types: [User, ServiceAccount]
direct_control: true
maps_to_primitives: [PasswordReset]
extra_conditions: [Downstream value depends on target privileges.]
